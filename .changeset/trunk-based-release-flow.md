---
---

Switch the contribution flow to a trunk-based model: feature branches and pull
requests target `main`, releases are driven entirely by the Changesets
"Version Packages" PR, and the `main` → `develop` back-merge workflow is
removed. Docs/CI-only change — no package release.
